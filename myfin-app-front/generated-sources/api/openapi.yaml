openapi: 3.1.0
info:
  title: OpenAPI definition
  version: v0
servers:
- description: Generated server url
  url: http://localhost:8200
tags:
- description: Operations related to reload data base account
  name: Api
paths:
  /api/uploadBase64Files:
    post:
      operationId: uploadBase64Files
      requestBody:
        content:
          application/json:
            schema:
              items:
                $ref: '#/components/schemas/FileBase64'
              type: array
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: string
          description: OK
      tags:
      - Api
      x-content-type: application/json
      x-accepts:
      - '*/*'
components:
  schemas:
    FileBase64:
      example:
        base64: base64
      properties:
        base64:
          type: string

